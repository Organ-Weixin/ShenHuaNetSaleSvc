package com.boot.security.server.utils;

import org.omg.IOP.Encoding;

public class HttpUtil {
	private final String sContentType = "application/x-www-form-urlencoded";
    private final String sUserAgent = "Mozilla/4.0 (compatible; MSIE 7.0; Windows NT 5.2; .NET CLR 1.1.4322; .NET CLR 2.0.50727)";
    /*public static String Send(String data, String url)
    {
        return Send(Encoding.GetEncoding("UTF-8").GetBytes(data), url);
    }*/
}
